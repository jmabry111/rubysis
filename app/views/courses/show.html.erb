<div class="center hero-unit">
	<h2 class="left">
		<%= @course.course_name %>
	</h2>
	<p>
		<strong>Description:</strong><br />
		<%= @course.course_description %>
	</p>
	<p><%= link_to "Add a course section", new_course_section_path(@course) %></p>
</div>